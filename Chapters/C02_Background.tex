%************************************************
\chapter{Background and Related Work} \label{ch:background}
%************************************************

This chapter establishes the background on which the thesis builds and reviews
the related work, moving from the setting in which the contribution is
demonstrated to the ideas from which it is built. \Cref{sec:background:nlp}
introduces \aclp*{LLM} and the transformer architecture that produces the dense
internal activations on which that demonstration rests.
\Cref{sec:background:xai} reviews explainable AI,
tracing the field's shift from attributing a prediction to input features
towards explaining it in terms of human-meaningful \textit{concepts}, the view
this work adopts. \Cref{sec:background:ae} then
develops the autoencoder family, from the standard autoencoder through the
variational and sparse variants between which the thesis's contribution sits. A
single tension runs through all three: reconstructing a model's activations
faithfully while decomposing them into a small, interpretable set of concepts
--- the tension the method of \cref{sec:methods:model}
sets out to relax.

\section{Natural Language Processing} \label{sec:background:nlp}

\ac{NLP} is the branch of machine learning concerned with getting computers to
process and generate human language. For most of its history the field advanced
through task-specific systems --- separate models for translation, sentiment
analysis, or question answering --- but it has since converged on a single
recipe: train one large neural network to predict text on a vast corpus, then
reuse it across tasks. The \acp{LLM} that result are the systems on which the
thesis's contribution is demonstrated. What follows introduces only as much of
their training and architecture as the later chapters rely on, with the emphasis
on the internal activations these models compute rather than the text they
emit.

\subsection{Language Modelling} \label{sec:background:nlp:lm}

A language model defines a probability distribution over sequences of text. In
the dominant \textit{autoregressive} formulation, this distribution is
factorised left to right, so that the model repeatedly predicts the next unit of
text given all the units before it \citep{radfordLanguageModelsAre2019}. The
units are \textit{tokens}, words or sub-word fragments drawn from a fixed
vocabulary, and the model is a neural network that maps a sequence of tokens to
a probability distribution over the vocabulary for the next position. Training
minimises the \ac{CE} between this predicted distribution and the true next
token, averaged over a large corpus; the objective requires no human annotation,
since the supervision signal is the text itself.

Scaling this recipe to more parameters, more data, and more computing power
yields \acp{LLM} whose capabilities extend well beyond the literal next-token
task they were trained on. A network trained only to continue text acquires the
ability to translate, summarise, answer questions, and follow instructions,
often from nothing more than a description and a few examples supplied in its
input, with no change to its weights \citep{brownLanguageModelsAre2020,
dongSurveyIncontextLearning2024}. This generality is what makes \acp{LLM} both
powerful and hard to interpret: a single fixed network comes to encode a vast
range of linguistic and world knowledge, distributed across its parameters and
activations in a form that no direct reading of the weights makes legible.

\subsection{The Transformer} \label{sec:background:nlp:transformer}

Modern \acp{LLM} are almost universally built on the \textit{transformer}
architecture \citep{vaswaniAttentionAllYou2017}. It embeds the input tokens into
a sequence of vectors, one per position, and refines them through a stack
of identical \textit{blocks} before reading the final vectors out as next-token
distributions. Each block is made of two sublayers with complementary roles: an
\textit{attention} sublayer, which exchanges information between positions, and
\iac{MLP} sublayer, which transforms each position on its own.
\Cref{fig:transformer-block} shows this organisation.

\begin{figure}[tb]
    \centering
    \begin{tikzpicture}[
        >={Stealth[round]},
        font=\small,
        block/.style={draw, rounded corners, align=center,
            minimum width=2.7cm, minimum height=0.9cm, fill=gray!8},
        op/.style={draw, circle, inner sep=0pt, minimum size=5mm, fill=gray!8},
        io/.style={align=center},
    ]
        \def\sx{0}      % residual-stream x
        \def\bx{3.5}    % sublayer-box x

        % spine: input, embedding, sublayer adds, unembedding, output
        \node[io]    (tokens) at (\sx,-0.9) {Input tokens
            $\langle t_{1}, \dots, t_{T}\rangle$};
        \node[block] (emb)    at (\sx,0.3)  {Token $+$ positional\\embedding};
        \node[op]    (add1)   at (\sx,2.4)  {$+$};
        \node[op]    (add2)   at (\sx,4.4)  {$+$};
        \node[block] (unemb)  at (\sx,6.2)  {Unembedding\\$+$ softmax};
        \node[io]    (out)    at (\sx,7.3)  {Next-token distribution};

        % sublayers
        \node[block] (attn) at (\bx,2.4) {Multi-head\\self-attention};
        \node[block] (mlp)  at (\bx,4.4) {MLP\\(position-wise)};

        % spine arrows
        \draw[->] (tokens) -- (emb);
        \draw[->] (emb)  -- (add1);
        \draw[->] (add1) -- (add2);
        \draw[->] (add2) -- (unemb);
        \draw[->] (unemb) -- (out);

        % sublayer read/write branches; name the turn points so the block
        % below can fit them
        \coordinate (aread) at (\sx,1.6);
        \coordinate (mread) at (\sx,3.6);
        \draw[->] (aread) -| (attn);   \draw[->] (attn) -- (add1);
        \draw[->] (mread) -| (mlp);    \draw[->] (mlp)  -- (add2);

        % one block, repeated N times; fitting the read-branch turn points
        % (aread/mread) makes the dashed box enclose the branch arrows too
        \begin{scope}[on background layer]
            \node[draw, dashed, rounded corners, fill=blue!4,
                  fit=(add1)(add2)(attn)(mlp)(aread)(mread),
                  inner sep=9pt] (blk) {};
        \end{scope}
        \node[anchor=west, font=\itshape] at (blk.east) {$\times\,N$};

        % stream label: tag the spine line itself (outside the block)
        \node[anchor=east, font=\footnotesize\itshape, text=gray!55!black]
            (rslabel) at (-0.9,5.45) {residual stream};
        \draw[->, gray!55!black] (rslabel) -- (\sx-0.1,5.45);
    \end{tikzpicture}
    \caption[A decoder-only transformer drawn around the residual stream]{
        A decoder-only transformer, drawn around the \textit{residual stream}.
        Each token is embedded into a vector; the resulting per-position state
        flows up the central stream, and every block reads from the stream and
        adds its result back through a residual connection --- first a
        multi-head self-attention sublayer, which mixes information across
        positions, then a position-wise \acs{MLP} sublayer. After $N$ such
        blocks the final stream state is unembedded into a next-token
        distribution. The additive structure of
        \cref{eq:background:nlp:residual} is what makes the
        residual stream a single, well-defined activation space to decompose;
        the view follows \citet{elhageMathematicalFrameworkTransformer2021}.
    }
    \label{fig:transformer-block}
\end{figure}

    \paragraph{Attention} \textit{Attention} is the mechanism by which positions
    exchange information. At each position, an \textit{attention head} forms a
    query and compares it against a key emitted by itself and every position
    before it; the better a position matches, the more it contributes to a
    weighted average of the corresponding values, which becomes the head's
    output for the current position \citep{vaswaniAttentionAllYou2017}.
    Concretely, the head projects each stream vector to a query, key, and value,
    $\vb{q}_t = Q \vb{x}_t$, $\vb{k}_s = K \vb{x}_s$, and
    $\vb{v}_s = V \vb{x}_s$, and returns
    \begin{equation} \label{eq:background:nlp:attention}
        \on{head}(\vb{x}_{1:t}) = \sum_{s=1}^{t} a_{ts} \vb{v}_s, \qquad
        a_{ts} = \on{softmax}_{s}\left(
        \frac{\vb{q}_t^{\top} \vb{k}_s}{\sqrt{d_k}}\right),
    \end{equation}
    where $d_k$ is the dimension of the queries and keys, and the softmax runs
    over $s = 1, \dots, t$, so that a position attends only to itself and those
    before it. A sublayer runs several such heads in parallel --- each free to
    specialise, one linking a pronoun to its referent, another a verb to its
    subject --- and combines their outputs by concatenation and a learned output
    projection. Because attention is the only component that moves information
    between positions, it is where the model assembles context: it turns a
    position's representation from a fact about a single token into one that
    reflects the surrounding text.

    \paragraph{MLP sublayers} Where attention mixes information across
    positions, \iac{MLP} sublayer processes each position on its own, applying
    the same feed-forward transformation to the vector at every position
    independently,
    \begin{equation} \label{eq:background:nlp:mlp}
        \on{MLP}(\vb{x}_t) = W_2\,
        \varphi\left(W_1 \vb{x}_t + \vb{b}_1\right) + \vb{b}_2,
    \end{equation}
    with a pointwise nonlinearity $\varphi$, an up-projection $W_1$ to a wider
    hidden layer and a down-projection $W_2$ back. Much of the model's
    capacity sits in these
    sublayers, which are widely thought to store and retrieve its learned
    associations and are a primary site at which interpretable features appear
    \citep{brickenMonosemanticityDecomposingLanguage2023}.

    \paragraph{The residual stream} A single detail ties these sublayers
    together. Neither sublayer overwrites the vector it is given; each computes
    an update and \textit{adds} it back, through a \textit{residual connection}.
    Every position therefore carries one running vector --- the \textit{residual
    stream} --- that every sublayer of every block reads from and writes to.
    Let $\vb{x}^{(\ell)}_{t}$ denote the residual stream at position $t$ as it
    enters block $\ell$; the block updates it as
    \begin{equation} \label{eq:background:nlp:residual}
        \vb{x}^{(\ell+1)}_{t} = \vb{x}^{(\ell)}_{t} +
        \on{Attn}^{(\ell)}\left(\vb{x}^{(\ell)}_{1:t}\right) +
        \on{MLP}^{(\ell)}\left(\vb{x}^{(\ell)}_{t}\right),
    \end{equation}
    where $\vb{x}^{(\ell)}_{1:t} = (\vb{x}^{(\ell)}_{1}, \dots,
    \vb{x}^{(\ell)}_{t})$ is the prefix of stream states up to position $t$ ---
    so the attention term, unlike the \ac{MLP} term, depends on the whole prefix
    rather than on $\vb{x}^{(\ell)}_{t}$ alone. The stream starts as the token's
    embedding and accumulates the output of every sublayer in turn; because
    attention writes into it, a position's stream is never a fact about that
    token alone but a running summary of the context around it. This additive
    structure is what makes the residual stream a single, well-defined space in
    which to study the model's computation --- the view developed in the
    mechanistic-interpretability literature
    \citep{elhageMathematicalFrameworkTransformer2021} and adopted throughout
    this thesis. It is also the object the later chapters take apart: the
    activation vector $\vb{x} \in \R^{n}$ that the autoencoders of
    \cref{sec:background:ae} receive as input is
    exactly such a residual stream state $\vb{x}^{(\ell)}$, read off at a chosen
    layer $\ell$.

% --------------------------------------------- %

\section{eXplainable AI} \label{sec:background:xai}

The incredible performance of modern machine-learning systems comes at the cost
of transparency: a deep network's output is the product of millions of
interacting parameters, with no accompanying account of why it was produced.
\acf{XAI} is the field that sets out to supply such accounts, making a model's
behaviour intelligible enough to be trusted, debugged, or learned from. The
methods relevant to this thesis are most naturally organised around a shift in
what counts as an explanation: from \textit{attribution}, which asks
\textit{where} (``which input features drove a prediction?''), to
\textit{concepts}, the human-meaningful units in terms of which a model can be
said to reason. It is this latter, concept-based view on which this work builds.

\subsection{Traditional XAI} \label{sec:background:xai:trad-xai}

\subsubsection{Intrinsically Interpretable Models}
\label{sec:background:xai:trad-xai:intrinsic}

The most direct route to an interpretable model is to choose one whose decision
process is transparent by construction. Linear and logistic regression, decision
trees, naive Bayes classifiers, and $k$-nearest neighbours are the canonical
examples: their parameters or structure can be inspected directly, so that the
explanation \textit{is} the model rather than a separate artefact derived from
it \citep{molnarInterpretableMachineLearning2025}.
\citet{rudinStopExplainingBlack2019} argues that such models should be preferred
wherever they are competitive, since post-hoc explanations of an opaque model
are approximations that need not be faithful to its actual computation. This
route is, however, closed for the systems studied in this thesis: \acp{LLM}
comprise billions of nonlinearly interacting parameters, and no direct reading
of their weights yields a human-understandable account of their behaviour.
Interpretability must therefore be recovered \textit{post hoc}, from a model
that is already trained and fixed.

\subsubsection{Post-hoc Attribution Methods}
\label{sec:background:xai:trad-xai:post-hoc}

The dominant post-hoc paradigm explains an individual prediction by
\textit{attributing} it to the input features, assigning each feature a score
that quantifies its contribution to the output. LIME
\citep{ribeiroWhyShouldTrust2016} does this by fitting a simple, interpretable
surrogate (typically a sparse linear model) to the behaviour of the black box in
a local neighbourhood of the instance being explained. SHAP
\citep{lundbergUnifiedApproachInterpreting2017} unifies this and several related
schemes under the Shapley value from cooperative game theory, yielding feature
attributions that are additive and satisfy a set of desirable axioms. For models
operating on images, gradient-based saliency maps
\citep{simonyanDeepConvolutionalNetworks2014} and their refinements such as
Grad-CAM \citep{selvarajuGradCAMVisualExplanations2017} produce a heatmap over
the input that highlights the regions most influential to the prediction. What
these methods share is also their central limitation: they indicate
\textit{where} in the input the model is sensitive, but not \textit{what} the
model has internally come to represent. An attribution map can mark a pixel or a
token as important without revealing the human-meaningful concept it
participates in: a gap that motivates the concept-based methods of
\cref{sec:background:xai:c-xai}.

    \paragraph{Counterfactual explanations} A counterfactual explanation answers
    a contrastive question: what is the smallest change to the input that would
    have produced a different, desired prediction
    \citep{wachterCounterfactualExplanationsOpening2017}? Rather than scoring
    the features of the present input, it points to a nearby instance on the
    other side of the decision boundary: ``had this applicant's income been
    slightly higher, the loan would have been granted''. Such explanations are
    attractive because they are actionable and require no access to the model's
    internals, only the ability to query it. Generating counterfactuals that are
    at the same time close to the original input and realistic is itself a
    modelling problem, one which generative models are naturally suited to
    solve.

\subsubsection{Prototypes}
\label{sec:background:xai:trad-xai:prototypes}

A complementary, example-based strategy explains a model not through feature
scores but through representative instances. A \textit{prototype} is a data
point that is typical of a region of the input space, chosen so that a small set
of such points summarises the data (or the model's behaviour upon it) in terms a
human can grasp by inspection \citep{kimExamplesAreNot2016}. The defining
characteristic, for the purposes of this thesis, is that a prototype in this
traditional sense is an \textit{input instance}: an actual or representative
example drawn from the data space.

\subsection{Concept-based XAI} \label{sec:background:xai:c-xai}

\ac{CXAI} answers the \textit{what} question that attribution leaves open:
instead of pointing to the inputs responsible for a prediction, it explains the
prediction in terms of high-level, human-meaningful \textit{concepts}
(``striped'', ``has wheels'', ``expresses uncertainty'') of the kind a person
would use to describe the same decision. Methods in this family differ chiefly
in whether the concepts are built into the model from the outset or recovered
from a model that was trained without them.

\subsubsection{Explainable-by-design Models}

One way to guarantee that concepts genuinely take part in a model's decision is
to make them an explicit, intermediate stage of the computation, so that the
prediction is forced to pass through a concept representation before reaching
the output.

    \paragraph{Concept Bottleneck Models} \acp{CBM}
    \citep{kohConceptBottleneckModels2020} insert exactly such a stage: the
    network first maps the input to a set of human-specified concepts and then
    predicts the label from those concepts alone. Because the output depends on
    the input only through the concept layer, the model's reasoning can be read
    off the activated concepts, and a user can intervene on them, correcting a
    mistaken concept and observing the effect on the prediction. The downside is
    supervision: training \iac{CBM} requires data annotated by human experts
    with concept labels, which is costly and ties the model to a fixed,
    predefined vocabulary.

    \paragraph{Concept Embedding Models} A known weakness of \acp{CBM} is an
    accuracy--interpretability trade-off: when the predefined concepts do not
    carry all the information the task requires, forcing the prediction through
    a narrow concept bottleneck degrades performance. \Acp{CEM}
    \citep{espinosazarlengaConceptEmbeddingModels2022} loosen the bottleneck by
    representing each concept not as a single scalar but as a pair of
    high-dimensional embeddings, one for its active and one for its inactive
    state. This recovers accuracy comparable to an unconstrained black box while
    retaining concept-level interpretability and the possibility of
    intervention. Like \acp{CBM}, these models require a concept-annotated
    dataset.

    \paragraph{Label-free Concept Bottleneck Models} \Acp{LFCBM}
    \citep{oikarinenLabelfreeConceptBottleneck2022} address the annotation cost
    directly. Rather than relying on a hand-labelled concept set, they extract
    candidate concepts from \iac{LLM} and align the network's activations with
    them through a vision--language model such as \acs{CLIP}, converting a
    standard backbone into \iac{CBM} without any concept supervision. This makes
    the approach scalable to settings where dense concept annotation would be
    impractical.

    \paragraph{Concept Bottleneck LLMs} The concept-bottleneck idea has recently
    been carried over to language. \Acp{CBLLM}
    \citep{sunConceptBottleneckLarge2025a} insert a concept bottleneck layer
    into \iac{LLM}, routing text classification --- and, in a generative
    variant, generation itself --- through a set of interpretable concepts
    before the output. As in \iac{LFCBM}, a hand-annotated concept set is not
    needed: the concepts are generated by a prompted \ac{LLM} and each training
    text is scored against them automatically, so the bottleneck is built
    without concept supervision. The explicit concept layer also makes the model
    \textit{steerable}, since intervening on a concept predictably shifts its
    behaviour. Like the other methods in this group, \acp{CBLLM} are
    \textit{explainable by design}: interpretability is secured by training the
    model with the concept bottleneck built in.

    Even so, that interpretability is only partial. In the generative variant
    the next-token prediction is shaped not by the concept bottleneck alone but
    by the bottleneck together with a parallel layer of unsupervised,
    uninterpretable neurons added to preserve generation quality. When the
    dataset labels are reused as concepts the bottleneck can be tiny --- 4
    neurons for the 4 AG News classes --- yet in the authors' implementation
    it runs alongside $768$ unsupervised ones,\footnote{The width of the
    unsupervised layer is fixed at $768$ in the authors' released code
    (\url{https://github.com/Trustworthy-ML-Lab/CB-LLMs}); the paper itself
    leaves it unspecified.} so only 4 of the $772$ dimensions feeding each token
    prediction carry interpretable concepts. Those interpretable dimensions
    remain intervenable nonetheless: because they feed the prediction directly,
    clamping or amplifying one steers the model's behaviour predictably --- the
    steerability the bottleneck is built for --- even if they carry only a
    fraction of the signal.

\subsubsection{Post-hoc Methods}

The alternative to \textit{explainable-by-design} models is to leave the trained
model untouched and probe it for the concepts it has already come to represent.

    \paragraph{Testing with Concept Activation Vectors} T-CAV
    \citep{kimInterpretabilityFeatureAttribution2018} represents a concept as a
    direction in a layer's activation space: given example inputs exhibiting the
    concept and a set of random ones, it fits a linear classifier to separate
    them and takes the normal to the decision boundary --- the concept
    activation vector --- as the concept's direction. The model's sensitivity to
    the concept is then quantified by the directional derivative of the output
    along this vector, yielding a global statement of how much a given concept
    influences a class prediction. The concept must still be supplied in
    advance, as a set of examples.

    \paragraph{SAEs and Mechanistic Interpretability} A final, unsupervised
    strand removes even that requirement. Rather than testing for concepts named
    ahead of time, \acp{SAE} learn a dictionary of concept-like features
    directly from a model's internal activations
    \citep{hubenSparseAutoencodersFind2023}, and have become a central tool of
    \ac{MI} --- the effort to reverse-engineer the computations of neural
    networks, and of \aclp{LLM} in particular. This unsupervised discovery of
    interpretable features is treated in full in \cref{sec:background:ae:sae}.

% --------------------------------------------- %

\section{AutoEncoders} \label{sec:background:ae}

An autoencoder is a neural network trained to reconstruct its input through an
intermediate latent representation \cite[Ch 14]{goodfellowDeepLearning2016}. It
consists of two components: an \textbf{encoder} $f_{\phi}: \R^{n} \rightarrow
\R^{m}$ that maps an input $\vb{x}$ to a latent code $\vb{z} =
f_{\phi}(\vb{x})$, and a \textbf{decoder} $g_{\theta}: \R^{m} \rightarrow
\R^{n}$ that reconstructs the input as $\hat{\vb{x}} = g_{\theta}(\vb{z})$.
Training minimises a reconstruction loss, typically the \ac{MSE}:
\begin{equation} \label{eq:background:ae:recon-loss}
    \mathcal{L}_{\mathrm{recon}}
    = \on{MSE}(\vb{x}, \,\hat{\vb{x}})
    = \frac{1}{n} \norm{\vb{x} - \hat{\vb{x}}}^{2}_{2}.
\end{equation}

When $m < n$, the bottleneck forces the model to learn a compressed
representation. When $m > n$, the network is overcomplete and additional
constraints are required to prevent the trivial solution of learning the
identity. In either case, the structure of the latent space is shaped by the
choice of architecture and regularisation.


% \subsection{Standard AutoEncoders} \label{sec:background:ae:standard}
%
% \subsubsection{Applications} \label{sec:background:ae:standard:applications}
%
%     \paragraph{Image retrieval} \citet{krizhevskyUsingVeryDeep2011} proposed
%     using deep autoencoders for content-based image retrieval. Their key insight
%     is that a deep autoencoder can learn to map images to short binary codes
%     that preserve semantic similarity: images that are perceptually similar are
%     mapped to nearby codes in the latent space, allowing retrieval to be reduced
%     to a fast lookup rather than an exhaustive search over the full database.
%     They demonstrate this on a dataset of 1.6 million tiny images, showing that
%     codes produced by deep autoencoders yield qualitatively and quantitatively
%     better retrieval results than matching in raw pixel space. This work
%     illustrated that the compressed latent representation learned by an
%     autoencoder need not merely serve reconstruction, but can itself be a
%     semantically meaningful and practically useful object.
%
%     \paragraph{Anomaly detection} \citet{sakuradaAnomalyDetectionUsing2014} were
%     among the first to explicitly propose autoencoders as a tool for anomaly
%     detection. Their core insight is that an autoencoder trained on normal data
%     learns a compressed representation of the normal data distribution; at test
%     time, inputs that deviate from this distribution are reconstructed poorly,
%     and the resulting reconstruction error serves as an anomaly score. To
%     substantiate this claim, they apply autoencoders to both synthetic data
%     generated from the Lorenz system and real spacecraft telemetry, comparing
%     against linear \ac{PCA} and kernel \ac{PCA}. They demonstrate that
%     autoencoders, by virtue of their nonlinear dimensionality reduction, are
%     able to detect subtle anomalies that linear \ac{PCA} misses, while remaining
%     computationally lighter than kernel \ac{PCA}. This work established
%     reconstruction error as a natural unsupervised anomaly signal for
%     autoencoder-based methods.
%
%
% \subsubsection{Denoising AutoEncoders} \label{sec:background:ae:standard:dae}
% The denoising autoencoder was introduced by
% \citet{vincentExtractingComposingRobust2008} to avoid overfitting and improve
% the robustness of the learned representation. The input is partially corrupted
% by adding noise to or masking some values of the input vector in a stochastic
% manner, $\vb{x}_{c} \sim \mathcal{M}_{\mathcal{D}}(\vb{x}_{c} \mid \vb{x})$, as
% shown in \cref{fig:denoising-ae}. The model is then trained to reconstruct the
% original uncorrupted input:
% \begin{equation} \label{eq:background:ae:dae-loss}
%     \mathcal{L}_{\mathrm{DAE}} = \norm{\vb{x} -
%     g_{\theta}(f_{\phi}(\vb{x}_{c}))}^{2}_{2}.
% \end{equation}
%
% \begin{figure}[tb]
%     \centering
%     \includegraphics[width=0.95\linewidth]{gfx/denoising-autoencoder-architecture.png}
%     \caption[Denoising autoencoder architecture]{
%         Illustration of Denoising autoencoder model architecture, reproduced
%         from \citet{wengAutoencoderBetaVAE2018}. \textit{Note: this figure uses
%         $f$ for the decoder and $g$ for the encoder, the reverse of the letter
%         convention adopted in this thesis.}
%     }
%     \label{fig:denoising-ae}
% \end{figure}
%
% This design is motivated by the fact that humans can easily recognise an object
% or a scene even when the view is partially occluded or corrupted. To ``repair''
% the partially destroyed input, the denoising autoencoder has to discover and
% capture the relationship between dimensions of the input in order to infer
% missing pieces.
%
% For high-dimensional input with high redundancy, like images, the model is
% likely to depend on evidence gathered from a combination of many input
% dimensions to recover the denoised version rather than to overfit one dimension.
% This builds up a good foundation for learning a \textit{robust} latent
% representation.
%
% The noise is controlled by a stochastic mapping
% $\mathcal{M}_{\mathcal{D}}(\vb{x}_{c} \mid \vb{x})$, and it is not specific to a
% particular type of corruption process (\ie masking noise, Gaussian noise,
% salt-and-pepper noise, etc.). Naturally the corruption process can be equipped
% with prior knowledge \cite{wengAutoencoderBetaVAE2018}.


\subsection{Variational AutoEncoders} \label{sec:background:ae:vae}

\acp{VAE} \cite{kingmaAutoencodingVariationalBayes2014,
rezendeStochasticBackpropagationApproximate2014} reinterpret the autoencoder as
a probabilistic generative model. Rather than mapping an input to a single
latent point, the encoder maps it to a \textit{distribution} over latent codes,
and the decoder is treated as a generative model that defines a distribution
over inputs given a latent code. This probabilistic framing turns the
autoencoder into a model from which new data can be sampled, and forces its
latent space to have a smooth, continuous structure.

This smoothness is the property that sets \iac{VAE} apart from a standard
autoencoder. Trained for reconstruction alone, an autoencoder is free to scatter
its encodings into well-separated clusters with large gaps between them; those
gaps are regions that decode to unrealistic outputs, so the space cannot be
sampled or interpolated reliably. The \ac{VAE} objective instead pushes
encodings to fill the latent space more uniformly and to vary smoothly, so that
nearby codes decode to similar inputs and paths between codes stay on the data
manifold. \Cref{fig:ae-vae-latent}
illustrates the difference on a two-dimensional latent space trained on MNIST.

\begin{figure}[tb]
    \centering
    \includegraphics[width=0.95\linewidth]{gfx/ae-vae-latent.png}
    \caption[Autoencoder and VAE latent spaces on MNIST]{
        Two-dimensional latent spaces learned on MNIST, coloured by digit class,
        reproduced from \citet{shafkatIntuitivelyUnderstandingVariational2018}.
        Optimising for reconstruction alone (left) lets a standard autoencoder
        separate the classes into disconnected clusters with gaps that decode to
        unrealistic samples, whereas the \acl{VAE} objective (right) yields a
        smoother, more continuous arrangement, concentrated around the origin by
        the prior, that can be sampled and interpolated.
    }
    \label{fig:ae-vae-latent}
\end{figure}

\subsubsection{Variational Inference} \label{sec:background:ae:vae:inference}
Formally, \iac{VAE} models the data through a generative process in which a
latent code is first drawn from a prior $\vb{z} \sim p(\vb{z})$ --- typically a
standard Gaussian $p(\vb{z}) = \mathcal{N}(\vb{0}, \mathbb{I})$ --- and an
observation is then drawn from a decoder distribution $\nbmath{p_{\theta}(\vb{x}
\mid \vb{z})}$ parameterised by a neural network. Training would ideally
maximise the marginal likelihood
\begin{equation*}
    p_{\theta}(\vb{x}) = \int p_{\theta}(\vb{x} \mid \vb{z}) \, p(\vb{z})
    \dd{\vb{z}},
\end{equation*}
but this integral, and the corresponding posterior $p_{\theta}(\vb{z} \mid
\vb{x}) = \flatfrac{p_{\theta}(\vb{x} \mid \vb{z})
p_{\theta}(\vb{z})}{p_{\theta}(\vb{x})}$, are intractable for any non-trivial
decoder.

The key idea of \citet{kingmaAutoencodingVariationalBayes2014} is to sidestep
this intractability with \textit{amortised variational inference}: an encoder
network $\nbmath{q_{\phi}(\vb{z} \mid \vb{x})}$ is trained to approximate the
true posterior, with all data points sharing the same inference parameters
$\phi$. The encoder outputs the parameters of a diagonal Gaussian,
\begin{equation} \label{eq:background:ae:vae:posterior}
    q_{\phi}(\vb{z} \mid \vb{x}) = \mathcal{N}\!\left(\vb{z};\,
    \boldsymbol{\mu}_{\phi}(\vb{x}),\,
    \on{diag}(\boldsymbol{\sigma}^{2}_{\phi}(\vb{x}))\right),
\end{equation}
so that each input is mapped to a mean and a (diagonal) covariance rather than
to a single point.

Instead of the intractable likelihood, the \ac{VAE} maximises a tractable lower
bound on it, the \ac{ELBO}:
\begin{equation} \label{eq:background:ae:elbo}
    \log p_{\theta}(\vb{x}) \ge \underbrace{\mathbb{E}_{q_{\phi}(\vb{z} \mid
    \vb{x})}\left[\log p_{\theta}(\vb{x} \mid
    \vb{z})\right]}_{\text{reconstruction}} -
    \underbrace{D_{\mathrm{KL}}\left(q_{\phi}(\vb{z} \mid \vb{x}) \,\big\|\,
    p(\vb{z})\right)}_{\text{regularisation}}.
\end{equation}
The bound follows from the identity
\begin{equation*}
    \log p_{\theta}(\vb{x}) = \mathrm{ELBO} +
    D_{\mathrm{KL}}\!\left(q_{\phi}(\vb{z} \mid \vb{x}) \,\big\|\,
    p_{\theta}(\vb{z} \mid \vb{x})\right),
\end{equation*}
together with the non-negativity of the \ac{KL} divergence; it is tight
precisely when the approximate posterior matches the true posterior
$\nbmath{p_{\theta}(\vb{z} \mid \vb{x})}$.
Maximising the \ac{ELBO} simultaneously pushes the decoder to assign high
likelihood to the data (the reconstruction term) and keeps the approximate
posterior close to the prior (the \ac{KL} divergence term). Negating it yields
the training loss:
\begin{equation} \label{eq:background:ae:vae-loss}
    \mathcal{L}_{\mathrm{VAE}} = \mathcal{L}_{\mathrm{recon}} +
    D_{\mathrm{KL}}\!\left(q_{\phi}(\vb{z} \mid \vb{x}) \,\big\|\,
    p(\vb{z})\right).
\end{equation}
When the decoder is Gaussian with fixed isotropic variance, the first term
reduces, up to an additive constant, to the \ac{MSE} reconstruction loss of
\cref{eq:background:ae:recon-loss}; the second term, being the
divergence between two Gaussians, has a closed form. For the standard-normal
prior it becomes:
\begin{equation*}
    D_{\mathrm{KL}}\!\left(q_{\phi}(\vb{z} \mid \vb{x}) \,\big\|\,
    p(\vb{z})\right) = \frac{1}{2} \sum_{j=1}^{m} \left(\mu_{j}^{2} +
    \sigma_{j}^{2} - \log \sigma_{j}^{2} - 1\right),
\end{equation*}
where $\mu_{j}$ and $\sigma_{j}$ are the components of the encoder's predicted
mean and standard deviation.

Evaluating the reconstruction term requires sampling $\vb{z}$ from
$\nbmath{q_{\phi}(\vb{z} \mid \vb{x})}$, an operation that is not differentiable
with respect to the encoder parameters and would therefore block gradient-based
training. The \ac{VAE} resolves this with the \textit{reparameterisation trick}:
the stochasticity is moved to an auxiliary noise variable, and the sample is
expressed as a deterministic, differentiable function of the encoder outputs,
\begin{equation*}
    \vb{z} = \boldsymbol{\mu}_{\phi}(\vb{x}) +
    \boldsymbol{\sigma}_{\phi}(\vb{x}) \odot \boldsymbol{\epsilon}, \quad
    \boldsymbol{\epsilon} \sim \mathcal{N}(\vb{0}, \mathbb{I}),
\end{equation*}
where $\odot$ denotes the element-wise product. Gradients can now flow through
$\boldsymbol{\mu}_{\phi}$ and $\boldsymbol{\sigma}_{\phi}$ while the
randomness is confined to $\boldsymbol{\epsilon}$, making the whole objective
trainable end-to-end by stochastic gradient descent.

\subsubsection{Architectures} \label{sec:background:ae:vae:arch}
There exist several variants that modify the base \ac{VAE} to shape the latent
space or the form of the latent code; two are particularly relevant here.

    \paragraph{$\beta$-VAE} \citet{higginsBetaVAELearningBasic2017} introduced a
    single modification: a scalar weight $\beta$ on the \ac{KL} term of the
    objective,
    \begin{equation} \label{eq:background:ae:vae:beta-loss}
        \mathcal{L}_{\beta\text{-VAE}} = \mathcal{L}_{\mathrm{recon}} + \beta \,
        D_{\mathrm{KL}}\!\left(q_{\phi}(\vb{z} \mid \vb{x}) \,\big\|\,
        p(\vb{z})\right).
    \end{equation}
    Setting $\beta > 1$ places additional pressure on the approximate posterior
    to match the factorised prior, which encourages individual latent dimensions
    to capture statistically independent factors of variation --- a property
    known as \textit{disentanglement}. The standard \ac{VAE} is recovered at
    $\beta = 1$. Changing $\beta$ offers a trade-off: a larger value improves
    the structure and interpretability of the latent space at the cost of
    reconstruction fidelity.

    \paragraph{VQ-VAE} \citet{vandenoordNeuralDiscreteRepresentation2017}
    replaced the continuous Gaussian latent with a \textit{discrete} one. The
    encoder output is quantised to the nearest entry of a learned codebook
    $\{\vb{e}_{1}, \dots, \vb{e}_{K}\}$, so that each latent is represented by a
    discrete index rather than a continuous vector. This sidesteps the
    \textit{posterior collapse} that can affect continuous \acp{VAE} paired with
    expressive autoregressive decoders, in which the decoder learns to ignore
    the latent entirely. Because the nearest-neighbour lookup is
    non-differentiable, gradients are passed to the encoder with a
    straight-through estimator and the codebook is learned through dedicated
    loss terms. \ac{VQVAE} underpins many modern generative systems, providing
    the discrete latent vocabulary over which an autoregressive prior is
    subsequently trained.

% \subsubsection{Applications} \label{sec:background:ae:vae:applications}
%
% \acp{VAE} are used both as practical components in large generative systems and
% for interpretable representation learning; the latter is the more relevant to
% this thesis.
%
%     \paragraph{Generative modelling} Although \iac{VAE} can act as a standalone
%     generator, in modern systems it more often serves as a \textit{compression
%     backbone} for other generative models: latent diffusion models run the
%     diffusion process in the compact latent space of an autoencoder rather than
%     in pixel space. The autoencoder behind Stable Diffusion
%     \cite{rombachHighResolutionImageSynthesis2022} is regularised only weakly,
%     with a small \ac{KL} penalty (in the manner of a \ac{VAE}) or with vector
%     quantisation. Discrete VQ-VAEs
%     \cite{vandenoordNeuralDiscreteRepresentation2017} encode data into a grid of
%     discrete codes from a learned codebook, which an autoregressive prior can
%     then generate. The smooth latent also serves as a search space: optimising
%     over it has been used to search for new molecules with desired properties
%     \cite{gomez-bombarelliAutomaticChemicalDesign2018}.
%
%     \paragraph{Anomaly detection} Like standard autoencoders, \acp{VAE} can flag
%     anomalies, but they replace the single reconstruction error with a
%     probabilistic score: the reconstruction probability, or the \ac{ELBO}, gives
%     a principled measure of how well the model explains an input
%     \cite{anVariationalAutoencoderBased2015}. This is the probabilistic
%     counterpart of the approach described in
%     \cref{sec:background:ae:standard:applications}.
%
%     \paragraph{Representation learning} \acp{VAE} are also a standard tool for
%     unsupervised representation learning. The $\beta$-VAE
%     (\cref{sec:background:ae:vae:arch})
%     is the canonical example: up-weighting the \ac{KL} term pressures the model
%     towards \textit{disentangled} latents whose individual dimensions capture
%     independent, semantically meaningful factors of variation
%     \cite{higginsBetaVAELearningBasic2017}, yielding structured low-dimensional
%     features for downstream use.
%
%     \paragraph{Interpretability} The structured latent that makes \acp{VAE} good
%     representation learners also makes them attractive for explainability. Their
%     smoothness supports \textit{counterfactual explanation}
%     (\cref{sec:background:xai:trad-xai:post-hoc}),
%     as perturbing the latent yields plausible, on-manifold variants of an input
%     \cite{joshiRealisticIndividualRecourse2019}. A substantial line of work uses
%     \acp{VAE} for \textit{causal representation learning}, aligning latent
%     factors with underlying causal variables
%     \cite{goyalExplainingClassifiersCausal2020,
%     komanduriSCMVAELearningIdentifiable2022}. \acp{VAE} have also been paired
%     with concept-based models such as self-explaining networks
%     \cite{alvarezmelisRobustInterpretabilitySelfExplaining2018}. Closest to this
%     thesis, variational ideas have been brought to sparse dictionary learning
%     --- variational sparse coding \cite{tonoliniVariationalSparseCoding2020} and
%     variational \aclp{SAE} \cite{bakerAnalysisVariationalSparse2025} --- seeking
%     to combine the interpretability of sparse codes with the structured latent
%     of a \ac{VAE}.


\subsection{Sparse AutoEncoders} \label{sec:background:ae:sae}

\acfp{SAE} are one of the main variants of autoencoders. Just like autoencoders,
they learn a dictionary of feature directions from the input data, but they
typically are overcomplete ($m \gg n$) and their latent representation is forced
to be sparse using some kind of sparsity mechanism. Given input $\vb{x} \in
\R^{n}$, the \ac{SAE} encodes it as:
\begin{equation} \label{eq:background:ae:sae:encode}
    \vb{z} = \operatorname{ReLU}(W_{e}(\vb{x} - \vb{b}_{\mathrm{pre}}) +
    \vb{b}_{e}),
\end{equation}
where $W_{e} \in \R^{m \times n}$ is the encoder weight matrix,
$\vb{b}_{e} \in \R^{m}$ is the encoder bias, and $\vb{b}_{\mathrm{pre}} \in
\R^{n}$ is a pre-encoder bias subtracted from the input before encoding. This
learned offset allows the encoder to operate on roughly mean-centred
activations, improving training stability
\cite{brickenMonosemanticityDecomposingLanguage2023}. The decoder then
reconstructs the input as:
\begin{equation} \label{eq:background:ae:sae:decode}
    \hat{\vb{x}} = W_{d} \vb{z} + \vb{b}_{\mathrm{pre}},
\end{equation}
where $W_{d} \in \R^{n \times m}$ is the decoder weight matrix. Each column
$\vb{d}_{i}$ of $W_{d}$ is a dictionary atom, representing a candidate
feature direction in activation space.

When used in the context of \ac{MI}, the overcompleteness and the sparsity
constraint are motivated by the \textit{superposition hypothesis}.

\subsubsection{The Superposition Hypothesis}
\label{sec:background:ae:sae:superposition}

A central challenge in \ac{MI} is that individual neurons in neural networks
tend to be \textit{polysemantic}: they activate in response to multiple,
seemingly unrelated concepts
\cite{elhageToyModelsSuperposition2022,olahZoomIntroductionCircuits2020}. An
example of this can be seen in
\cref{fig:polysemanticity}.
This runs contrary to the intuition that an interpretable model should have
neurons with clean, well-defined roles.

\begin{figure}[tb]
    \centering
    \includegraphics[width=0.95\linewidth]{gfx/polysemanticity.png}
    \caption[A polysemantic neuron in InceptionV1]{
        Neuron 4e:55, a polysemantic neuron from InceptionV1 which responds to
        cat faces, fronts of cars, and cat legs. Reproduced from
        \citet{olahZoomIntroductionCircuits2020}.
    }
    \label{fig:polysemanticity}
\end{figure}

\citet{elhageToyModelsSuperposition2022} proposed the \textit{superposition
hypothesis} to explain this phenomenon. The core idea is that a network may
represent more features than it has dimensions by exploiting the fact that
natural inputs are sparse: at any given time, only a small fraction of all
possible features are relevant. Under this condition, a network can pack many
near-orthogonal feature directions into a lower-dimensional space with limited
interference; polysemanticity ceases to be a failure of the model, and instead
becomes a rational compression strategy under capacity constraints.

This has a direct implication for interpretability: the natural basis of a
model's activations is not the neuron basis, but a larger, overcomplete set of
feature directions embedded within it. Recovering this basis requires moving
beyond individual neuron analysis, towards methods that can identify sparse,
distributed structure. This is precisely the goal of \textit{sparse dictionary
learning} \cite{olshausenSparseCodingOvercomplete1997}, a classical technique
from computational neuroscience that \acp{SAE} adapt to the setting of modern
language models.

\subsubsection{Architectures} \label{sec:background:ae:sae:arch}
There are many ways to enforce sparsity, each with its pros and cons. Here is a
selection of methods that range from the initial \ac{SAE} idea to more modern
architectures that try to improve on it.

    \paragraph{Vanilla $\ell_{1}$} This is the original architecture proposed by
    \citet{hubenSparseAutoencodersFind2023}. It uses an $\ell_{1}$
    penalty\footnote{The $\ell_{p}$ norm is defined as $\ell_{p}(\vb{x}) =
    \norm{\vb{x}}_{p} = \left(\sum_{i}
    \abs{x_{i}}^{p}\right)^{\flatfrac{1}{p}},\ p \ge 1$.} on the latent code to
    encourage most of $\vb{z}$'s entries to be exactly zero, so that any given
    activation is reconstructed from a small number of active features. The loss
    used to train this model is the following:
    \begin{equation} \label{eq:background:ae:sae:l1-loss}
        \mathcal{L} = \mathcal{L}_{\mathrm{recon}} + \lambda \norm{\vb{z}}_{1}.
    \end{equation}
    The scalar $\lambda$ controls the sparsity--reconstruction trade-off and
    must be tuned carefully: too large, and reconstruction degrades; too small,
    and the learned features lose monosemanticity. A known side effect of
    $\ell_{1}$ regularisation is \textit{shrinkage}
    \cite{wrightAddressingFeatureSuppression2024,
    tibshiraniRegressionShrinkageSelection1996}: beyond driving inactive
    features to zero, the penalty also biases active feature magnitudes below
    their reconstruction-optimal values, causing the model to systematically
    underestimate the true activation of each feature
    \cite{rajamanoharanImprovingSparseDecomposition2024,
    wrightAddressingFeatureSuppression2024}. Constraining the decoder columns to
    unit $\ell_{2}$ norm after each gradient step partially mitigates this, by
    preventing the model from reducing the $\ell_{1}$ penalty through the
    trivial reparameterisation of scaling down encoder outputs and compensating
    with larger decoder norms
    \cite{rajamanoharanImprovingSparseDecomposition2024}. There is, however, a
    residual gradient-level bias that is structurally entangled with the
    $\ell_{1}$ penalty and cannot be resolved with this trick.

    \paragraph{TopK SAEs} \citet{gaoScalingEvaluatingSparse2024} proposed
    replacing the $\ell_{1}$ penalty with a hard sparsity constraint,
    implemented via a TopK activation function that retains only the $k$ largest
    pre-activations and zeros the rest. This directly controls the number of
    active features per forward pass, eliminating the need to tune $\lambda$ and
    removing the shrinkage bias entirely. The full training objective is:
    \begin{equation} \label{eq:background:ae:sae:topk-loss}
        \mathcal{L} = \mathcal{L}_{\mathrm{recon}} + \alpha
        \mathcal{L}_{\mathrm{aux}},
    \end{equation}
    where the auxiliary loss $\mathcal{L}_{\mathrm{aux}}$ addresses the problem
    of dead latents, \ie features that cease to activate entirely during
    training. Latents are flagged as dead if they have not activated for a
    predetermined number of tokens. Given the reconstruction residual $\vb{e} =
    \vb{x} - \hat{\vb{x}}$, the top-$k_{\mathrm{aux}}$ dead latents are selected
    and used to reconstruct $\vb{e}$; the resulting reconstruction error is
    $\mathcal{L}_{\mathrm{aux}}$. This gives dead latents a gradient signal
    proportional to how much of the residual they could explain, encouraging
    them to activate rather than remain permanently dormant.
    \citet{gaoScalingEvaluatingSparse2024} demonstrate that TopK \acp{SAE}
    achieve better reconstruction at equivalent sparsity levels compared to
    $\ell_{1}$ baselines, making them a strong and widely used alternative.

    \paragraph{Gated SAEs} \citet{rajamanoharanImprovingSparseDecomposition2024}
    introduced an architecture that separates the decision of whether a feature
    is active from the decision of how strongly it activates. A dedicated gating
    pathway produces a binary mask via a Heaviside step function (approximated
    during training with a straight-through estimator) which is applied
    element-wise to the encoder output. This architectural separation allows the
    model to learn feature presence and magnitude more independently, addressing
    shrinkage from a structural rather than a loss-based perspective.

    \paragraph{JumpReLU SAEs} \citet{rajamanoharanJumpingAheadImproving2024}
    proposed replacing the standard ReLU with a JumpReLU activation, which is
    zero below a learned per-feature threshold $\theta_{i}$ and equal to the
    pre-activation above it:
    \begin{equation} \label{eq:background:ae:sae:jumprelu}
        \operatorname{JumpReLU}_{\theta_{i}}(z_{i}) = z_{i} \cdot
        \mathds{1}[z_{i} > \theta_{i}].
    \end{equation}

    The key advantage of this parameterisation is that it provides a dedicated,
    per-feature learnable threshold, which allows the $\ell_{0}$
    norm\footnote{The $\ell_{0}$ norm is a pseudo-norm defined as the number of
    non-zero vector components: $\ell_{0}(\vb{x}) = \sum_{i} \mathds{1}[x_{i} >
    0]$.} of the feature activations --- $\sum_{i} H(\pi_{i} - \theta_{i})$,
    where $\pi_{i}$ are the encoder pre-activations and $H(x)$ is the Heaviside
    step function --- to be trained directly via a straight-through estimator
    applied to the threshold parameter $\theta_{i}$. Because each threshold can
    be positioned near the actual boundary between active and inactive
    pre-activations for its feature, the gradient signal is stable and
    controllable, making $\ell_{0}$ training practical in a way that would not
    be possible with a fixed threshold. Training with $\ell_{0}$ rather than
    $\ell_{1}$ eliminates shrinkage entirely, since $\ell_{0}$ penalises only
    the number of active features and not their intensity. JumpReLU \acp{SAE}
    achieve comparable performance to TopK \acp{SAE}.


% TODO: if including sections about AE and VAE applications, remove "to MechInt"
% and reword initial sentence
\subsubsection{Applications to MechInt} \label{sec:background:ae:sae:applications}

% Unlike general-purpose autoencoders and \acp{VAE}, which are applied across a
% range of domains, \acp{SAE}
\Acp{SAE} find their primary application in interpreting the
internal activations of neural networks, and \ac{MI} remains their most
prominent application, making it the focus of this section. \acp{SAE} can be
trained on activations from any layer or component of a transformer
(\cref{sec:background:nlp:transformer});
common targets include the residual stream at various depths, \ac{MLP} layer
outputs, and attention head outputs \cite{hubenSparseAutoencodersFind2023,
brickenMonosemanticityDecomposingLanguage2023}. The resulting dictionary then
supports the applications described below.

    \paragraph{Feature discovery} The primary use of \acp{SAE} is
    \textit{feature discovery}: training \iac{SAE} on a model's internal
    activations and examining the resulting dictionary atoms to identify
    human-interpretable concepts. The underlying hypothesis is that each atom,
    active for only a semantically coherent set of inputs, corresponds to a
    monosemantic feature of the model's computation --- a clean unit of meaning
    that the model had obscured in the neuron basis. In practice these features
    often correspond to recognisable concepts:
    \citet{brickenMonosemanticityDecomposingLanguage2023} trained \iac{SAE} on
    the \ac{MLP} of a one-layer transformer and found features responsive to
    specific tokens, syntactic roles, and semantic categories, while at
    production scale \citet{templetonScalingMonosemanticityExtracting} report
    features for famous people, countries, emotions, or even biases (like
    racism, sexism, hatred) and sarcastic praise. These results provide strong
    empirical support for the superposition hypothesis and suggest that
    \acp{SAE} can serve as a practical lens through which to study what
    information \acp{LLM} represent.

    \paragraph{Steering} The features recovered by \iac{SAE} are not merely
    descriptive: because each corresponds to a direction in activation space,
    its activation can be clamped to an arbitrary value during a forward pass to
    causally \textit{steer} the model's behaviour.
    \citet{templetonScalingMonosemanticityExtracting} demonstrated this by
    amplifying a single feature, causing the model to compulsively reference the
    Golden Gate Bridge regardless of the input. Such interventions show that the
    discovered features are causally implicated in the model's computation
    rather than merely correlated with its inputs, and open a path to
    controllable generation and targeted safety interventions.

    \paragraph{Circuit analysis} Beyond cataloguing individual features,
    \acp{SAE} can serve as the building blocks for reconstructing the
    \textit{circuits} a model uses to perform a computation, expressing the
    interactions between features across layers as an interpretable causal graph
    \cite{marksSparseFeatureCircuits2025}. More recent work favours
    \textit{transcoders} --- a variant trained to approximate the input--output
    behaviour of an \ac{MLP} sublayer with a sparse hidden representation,
    rather than to reconstruct a single activation --- which should disentangle
    the model's computation more cleanly and yield sparser, more faithful
    circuits \cite{dunefskyTranscodersFindInterpretable2024,
    ameisenCircuitTracingRevealing2025}.

\subsubsection{Evaluation} \label{sec:background:ae:sae:evaluation} Assessing
the quality of learned features is non-trivial, as interpretability is
inherently subjective. Several proxy metrics are used in practice.
\textit{Reconstruction quality} is judged by its effect on the model rather than
by the raw reconstruction error: the \ac{SAE}'s output is substituted for the
original activations, the model is run forward, and the resulting increase in
its next-token \ac{CE} loss is measured \cite{gaoScalingEvaluatingSparse2024}. A
faithful \ac{SAE} leaves this loss almost unchanged, indicating that it has
retained the information the model actually uses. \textit{Sparsity} is
quantified via the average $\ell_{0}$ norm of the latent codes, \ie the mean
number of active features per token. \textit{Automated interpretability}
pipelines use a second language model to generate and score natural-language
descriptions of individual features \cite{billsLanguageModelsCan2023}, providing
a scalable if imperfect proxy for human judgement.

% --------------------------------------------- %

\section*{Conclusions}

This chapter has assembled the three strands the thesis builds on: the
transformer and its residual stream
(\cref{sec:background:nlp}), whose
dense, polysemantic activations are the object to be decomposed; the shift in
explainability from attribution to concepts
(\cref{sec:background:xai}), and the
concept-based methods that describe a model's computation in human-meaningful
terms; and the autoencoder family
(\cref{sec:background:ae}), from the variational
and discrete latents of \acp{VAE} and \acp{VQVAE} to the overcomplete, sparse
dictionaries of \acp{SAE}.

A single limitation runs through these concept-based methods. Whether a concept
is a unit in the bottleneck of \iac{CBM} or an atom in the dictionary of
\iac{SAE}, it is encoded as a single scalar and can vary only in magnitude. This
is what forces the sparsity--reconstruction trade-off at the heart of the
\ac{SAE} literature --- a faithful reconstruction needs many active concepts, an
interpretable decomposition only few --- and the architectures of
\cref{sec:background:ae:sae:arch} all
work within it. \Acp{CEM} are the exception already met: by giving each concept
a vector embedding rather than a scalar, they recover the capacity a scalar
bottleneck gives up. \Cref{sec:methods:model}
carries that idea into the unsupervised
setting of activation decomposition --- combining the vector-valued concepts of
\acp{CEM} with the variational inference and discrete latents developed in
\cref{sec:background:ae} --- and derives the
resulting model from first principles.


%*****************************************
%*****************************************
%*****************************************
%*****************************************
%*****************************************
