%************************************************
\chapter{Concept Dictionaries} \label{ch:app:concept-dict}
%************************************************

% TODO: populate with a RANDOM (not curated) sample of concept dictionaries —
% roughly 20–30 concepts per model, from the same matched pair shown in
% sec:exp:results:qual (the 128×64 VAEE at κ̃=8 and the m=8192 TopK SAE at k=8).
% A random sample (not hand-picked) is the point: it lets a reader verify
% first-hand the population claims that the three curated examples in C04 only
% illustrate — the "majority of uninterpretable units" for the VAEE and the
% "redundancy that consumes much of the dictionary" for the SAE. Reuse the
% tabularx concept-card format of tab:exp:qual:{vaee,sae}. Once populated, add a
% forward \cref{ch:app:concept-dict} from sec:exp:results:qual so the appendix is
% actually referenced (currently nothing points to it).

